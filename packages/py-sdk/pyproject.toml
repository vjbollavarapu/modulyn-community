[project]
name = "Modulyn-py-sdk"
version = "0.1.0"
description = "Minimal Python SDK for interacting with Modulyn ink! contract"
authors = [
  { name="Modulyn Community", email="team@Modulyn-erp.com" }
]
requires-python = ">=3.10"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"